<%= stylesheet_link_tag '/plugin_assets/redmine_task_manager/stylesheets/selectize.default.css' %>
<%= javascript_include_tag '/plugin_assets/redmine_task_manager/javascripts/selectize' %>
<p>
  <label><%= l('settings.allowed_groups_to_use_task_manager') %></label>
    <%= select_tag "settings[groups][]",
      options_from_collection_for_select(Group.all.sort, 'id', 'lastname', @settings['groups']
        ), :multiple => true, class: "selectize" %>

</p>
<p>
<style>
  div.box {
    overflow: initial;
  }
</style>
<script type="text/javascript">
  $(document).ready(function(){
    $('.selectize').selectize({
      plugins: ['remove_button']
    });
  });
</script>
